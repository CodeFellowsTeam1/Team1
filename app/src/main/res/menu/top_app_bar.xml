<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/profile"
        android:title="Profile"
        android:contentDescription="Profile"
        app:showAsAction="never" />

    <item
        android:id="@+id/trips"
        android:title="Trips"
        android:contentDescription="trips"
        app:showAsAction="never">

        <menu>
            <item
                android:id="@+id/currentTrips"
                android:title="Current Trip"
                android:contentDescription="current trips"
                app:showAsAction="never" />
            <item
                android:id="@+id/pastTrips"
                android:title="Past Trips"
                android:contentDescription="past trips"
                app:showAsAction="never" />
        </menu>
    </item>

    <item
        android:id="@+id/map"
        android:title="Map"
        android:contentDescription="map"
        app:showAsAction="never" />
</menu>